public class PolymorhpismPrac {
    // Poly - Many
    // Morph - Changes/Shapes/Forms

    public static int favoriteNumber(String studentName) {
        // Lookup student by name from DataBase
        // Return students favortieNumber
    }

    public static int favoriteNumber(int studentID) {
        // lookup student by id fom database
        // returns students favoriteNumber
    }

    // what is the diference between runtime and compiletime Polymorhpism

    // class Animal
    // public makeNoise(){...}
    // class cat extends Animal
    // public makeNoise(){...}
    // class dog extends Animal
    // public makeNoise(){...}

    // Animal arr = {new Dog(), new Cat(), new Dog()}

    // for(Animal a : arr) {
    // a.makeNoise();
    // }
}
